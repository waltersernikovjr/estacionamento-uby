# 🤖 AI Code Generation Guidelines

## ❌ O QUE NÃO FAZER

### Comentários Redundantes
**NUNCA** gerar comentários inline óbvios como:

```php
// Create user
$user = User::create($data);

// Update status
$status = 'active';

// Return response
return response()->json($user);

// Arrange (em testes)
$dto = new UserDTO();

// Act (em testes)
$result = $service->create($dto);

// Assert (em testes)
$this->assertEquals('active', $result->status);
```

### Comentários Aceitáveis

✅ **PHPDoc para documentação de API:**
```php
/**
 * Create a new user in the system.
 *
 * @param CreateUserDTO $dto
 * @return User
 * @throws \InvalidArgumentException
 */
public function create(CreateUserDTO $dto): User
{
    // ...
}
```

✅ **Explicação de lógica complexa:**
```php
// Calculate parking fee: R$5/hour + R$1 per additional 15min block
$hours = floor($minutes / 60);
$remainingMinutes = $minutes % 60;
$blocks = ceil($remainingMinutes / 15);
$amount = ($hours * 5.00) + ($blocks * 1.00);
```

✅ **Avisos importantes:**
```php
// WARNING: This operation is irreversible
$this->deleteAllUserData($userId);

// TODO: Implement caching for this endpoint
return $this->fetchFromDatabase();
```

## ✅ O QUE FAZER

### Código Auto-explicativo
Escreva código limpo que não precisa de comentários:

```php
// ❌ BAD
// Check if user is admin
if ($user->role === 'admin') {
    // ...
}

// ✅ GOOD
if ($user->isAdmin()) {
    // ...
}
```

### Nomes Descritivos
```php
// ❌ BAD
// Get data
$d = $repo->get();

// ✅ GOOD
$activeUsers = $repository->findActiveUsers();
```

### Testes Limpos
```php
// ❌ BAD
public function test_user_creation(): void
{
    // Arrange
    $dto = new CreateUserDTO('John', 'john@test.com');
    
    // Act
    $user = $this->service->create($dto);
    
    // Assert
    $this->assertEquals('John', $user->name);
}

// ✅ GOOD
public function test_user_creation(): void
{
    $dto = new CreateUserDTO('John', 'john@test.com');
    
    $user = $this->service->create($dto);
    
    $this->assertEquals('John', $user->name);
}
```

## 📋 Checklist de Código

Antes de gerar código, verificar:
- [ ] Código é auto-explicativo?
- [ ] Nomes de variáveis/métodos são claros?
- [ ] Comentários são realmente necessários?
- [ ] PHPDoc está completo apenas onde necessário?
- [ ] Não há comentários de "passos" (Step 1, Step 2)?
- [ ] Não há comentários AAA em testes?

## 🎯 Filosofia

**"Clean code doesn't need comments. Comments are apologies for bad code."**

- Prefira refatorar código confuso do que adicionar comentários
- Use nomes descritivos em vez de abreviações + comentários
- Extraia métodos com nomes claros em vez de comentar blocos de código
- Comentários devem explicar "por quê", não "o quê"

---

**Este arquivo serve como guideline para futuras gerações de código neste projeto.**
